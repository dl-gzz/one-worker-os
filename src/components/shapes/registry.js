import { QuizShapeUtil } from './QuizShape';
import { CameraSimulatorShapeUtil } from './CameraSimulatorShape';
import { CodeRunnerShapeUtil } from './CodeRunnerShape';
import { BrowserShapeUtil } from './BrowserShape';
import AIShapeGeneratorShapeUtil from './AIShapeGeneratorShape';
import { AITerminalShapeUtil } from './AITerminalShape';
import { WeatherShapeUtil } from './WeatherShape';

// 导出所有 Shape 工具类
export const customShapeUtils = [
    QuizShapeUtil,
    CameraSimulatorShapeUtil,
    CodeRunnerShapeUtil,
    BrowserShapeUtil,
    AIShapeGeneratorShapeUtil,
    AITerminalShapeUtil,
    WeatherShapeUtil,
    // AI_INSERT_POINT (AI 将在这里插入新的 Shape)
];
